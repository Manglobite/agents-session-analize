# Example target orchestrator prompt

Replace this file with, or add alongside it, the actual orchestrator prompt you want to evaluate.

Example hard rule:
- Shell operations must be delegated to the coding subagent. The orchestrator must not call shell tools directly.
